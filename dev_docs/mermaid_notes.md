# Mermaid in markdown files embeddings
Each of the embedding starts with

```mermaid
<mermaid diagram>
```


### erDiagram (ERD diagram) cardinality symbols:

<first_side>--<second_side>

first_side and second_side are the first emntity and second accordingly

Cardinality symbols (used at right side, id used at left flipped hirizontally):
|| Exactly one (mandatory)
|{ One or more (mandatory)
o| Zero or one (optional)
o{ Zero or more (optional)